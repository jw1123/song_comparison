============
Installation
============

At the command line::

    $ easy_install song_comparison

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv song_comparison
    $ pip install song_comparison